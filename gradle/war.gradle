apply plugin: "war"

bootWar {
    mainClassName = "com.jacinto.fas.JacintoFincaAdminServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
